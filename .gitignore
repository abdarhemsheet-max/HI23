node_modules/
dist/
*.local
.env
*.tsbuildinfo
.DS_Store

# نسخة SQLite القديمة والملفات المرفوعة محلياً قبل الهجرة — مرجع/نسخة احتياطية فقط،
# لا يقرأها التطبيق بعد الآن (البيانات انتقلت إلى Supabase + Backblaze B2)
prisma/dev.db*
uploads/
